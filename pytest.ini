[pytest]
addopts = -v --tb=short
testpaths = tests
markers =
    smoke: basic functionality tests
    regression: full end-to-end tests
    slow: long-running tests
    api: API tests
    ui: UI tests